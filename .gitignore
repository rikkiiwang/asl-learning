# OS / editor
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/

# JS
node_modules/
dist/

# Model weights / large data / caches (manifests, configs, reports stay tracked)
*.onnx
*.pt
*.pth
*.npz
model-v2/data/
model-v2/artifacts/cache/
model-v2/artifacts/checkpoints/
model-v2/artifacts/*.onnx

# Large local-only artifacts
*.zip
frames.dat

# Audit frame PNGs (large/binary; stub JSON is committed separately)
model-v2/artifacts/audit/frames/
